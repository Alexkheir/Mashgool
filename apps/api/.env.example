# ─── Mashgool API — environment variables ────────────────────────────────────
# Copy to `.env` for running the API outside Docker. Inside Docker these are
# provided by docker-compose.yml.

# Port the Express API listens on
PORT=4000
