# ─── Mashgool Web — environment variables ────────────────────────────────────
# Copy to `.env.local` for running the web app outside Docker. Inside Docker
# these are provided by docker-compose.yml.

# Base URL the BROWSER uses to reach the Express API
NEXT_PUBLIC_API_URL=http://localhost:4000
